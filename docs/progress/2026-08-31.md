# 2026-08-31 开发进度

## 今日目标

- [x] 初始化工程环境与基础骨架（Foundation 阶段）
- [x] 根 workspace、统一 TS/ESLint/Prettier/Vitest 配置
- [x] contracts 共享契约与单元测试
- [x] db（Drizzle Schema + migration）与 observability（Pino 工厂）
- [x] API 健康检查与注入测试
- [x] Worker 队列骨架与优雅关闭
- [x] Web Foundation 页面
- [x] Docker Compose（PostgreSQL + Redis）验证
- [x] CI、README、架构文档
- [x] 全量质量检查（format/lint/typecheck/test/build）

## 完成记录

### 任务 1：根工作区和规范配置

- 状态：完成
- 完成内容：初始化 Git 仓库（main 分支）、pnpm workspace（catalog 统一版本）、TypeScript 5.9 strict 基础配置、ESLint 10 扁平配置（typescript-eslint 类型感知规则）、Prettier、`.gitignore`、`.env.example`、`.npmrc`（engine-strict）。根脚本含 dev/build/typecheck/lint/format/format:check/test/db:generate/db:migrate。
- 关键文件：`package.json`、`pnpm-workspace.yaml`、`tsconfig.base.json`、`eslint.config.mjs`、`prettier.config.mjs`、`.gitignore`、`.npmrc`、`.env.example`
- 技术决策：
  1. **TypeScript 固定 5.9.3**：当前 latest 是 7.0.2（原生重写版），但 typescript-eslint 8.x peer 范围为 `<6.1.0`，为稳定选择 5.9.3。
  2. **本地目录为 `/Users/linghang/agent/PatchFlow`**：macOS APFS 默认不区分大小写，`/Users/linghang/agent/patchflow` 与已存在的 `PatchFlow`（含项目说明.md）是同一目录，因此直接在现有目录初始化，保留 `项目说明.md` 与 `.claude/`。
  3. **本地 Node 为 v25.7.0**（机器无版本管理器，未强行安装 Node 24）：engines 声明 `node >=24`，CI 固定 Node 24 LTS，本地 25 满足约束。
  4. pnpm 通过 `npm i -g pnpm` 安装（Node 25 不再内置 corepack），版本 11.24.0，记录在 `packageManager` 字段。
  5. **catalog 统一版本**：typescript/vitest/zod/pino 等跨包依赖集中在 `pnpm-workspace.yaml` catalog，各包用 `catalog:` 引用，避免版本漂移。
- 验证方式：`pnpm install`（生成 `pnpm-lock.yaml`）；`pnpm --version`
- 验证结果：install 成功（20.4s），根 devDependencies 全部安装；首次安装因 `@eslint/js@10.9.1` 不存在失败一次，改为 10.0.1 后通过。
- 遗留问题：无

### 任务 2：contracts 共享契约包

- 状态：完成
- 完成内容：Zod 契约 `RunStatusSchema`（11 个状态全量）、`RunPhaseSchema`（7 个执行阶段）、`RunLimitsSchema`、`CreateRunInputSchema`、`RunEventSchema`（10 种事件类型、UUID、正整数 sequence、ISO 时间、payload 默认空对象）；另含 `BaseEnvSchema` + `parseEnv`/`formatZodError` 供各服务入口统一校验环境变量。区分 status（生命周期唯一权威）与 phase（执行期细分）两个概念。
- 关键文件：`packages/contracts/src/run.ts`、`packages/contracts/src/event.ts`、`packages/contracts/src/env.ts`、`packages/contracts/src/index.ts`、`packages/contracts/src/{run,event,env}.test.ts`
- 技术决策：
  1. status 覆盖全部 11 个状态（含执行中与终态），phase 仅含 7 个执行阶段并拒绝 `queued`/终态——与项目说明第 7 节状态机一致，runs 表两列分开存储。
  2. `limits`、`payload`、`baseRef` 使用 Zod default，输入可省略、输出必填。
  3. createdAt 用 `z.iso.datetime()`（ISO 8601 字符串）作为线上契约，DB 层再转换为 Date。
- 验证方式：`cd packages/contracts && pnpm typecheck && pnpm test`
- 验证结果：typecheck 通过；3 个测试文件 40 个用例全部通过（Test Files 3 passed, Tests 40 passed）。
- 遗留问题：无

### 任务 3：db 包（Drizzle Schema + migration + 惰性客户端）

- 状态：完成
- 完成内容：`runs` 与 `run_events` 两张最小表（pg 枚举直接复用 contracts 常量）；`(run_id, sequence)` 唯一约束 + `run_id` 索引 + 外键级联删除；`createDb()` 工厂（pg.Pool 惰性连接，导入时零连接）；`migrate.ts` 支持命令行执行；生成第一份 migration `0000_odd_groot.sql`。结构测试验证表/列/约束与契约一致。
- 关键文件：`packages/db/src/schema.ts`、`packages/db/src/client.ts`、`packages/db/src/migrate.ts`、`packages/db/src/schema.test.ts`、`packages/db/drizzle.config.ts`、`packages/db/drizzle/0000_odd_groot.sql`
- 技术决策：pg 枚举（`run_status`/`run_phase`/`run_event_type`）由 contracts 常量生成，保证 DB 枚举与 Zod 契约不会漂移；`phase` 可空（queued 阶段无执行阶段）；`.env` 通过 `process.loadEnvFile` 加载（仓库根 + 包目录），不引入 dotenv 依赖。
- 验证方式：`pnpm -r --if-present typecheck`、`pnpm -r --if-present test`、`pnpm db:generate`
- 验证结果：db 包 typecheck 通过（曾因外键 `reference()` 用法错误失败一次，已修复）；13 个结构测试通过；migration 生成成功，SQL 含 `CONSTRAINT "run_events_run_id_sequence_key" UNIQUE("run_id","sequence")` 与 `ON DELETE cascade`。
- 遗留问题：真实数据库上的 migrate 执行待 Docker Compose 就绪后验证（见任务 7）。

### 任务 4：observability 包（Pino Logger 工厂）

- 状态：完成
- 完成内容：`createLogger()` 工厂：LOG_LEVEL 环境变量控制级别（非法值抛可读错误）；非 production 默认 pino-pretty 可读输出，production 输出 JSON；内置 18 条脱敏路径（password/apiKey/token/secret/authorization/connectionString 等及其嵌套形式）；支持注入 destination 便于测试。
- 关键文件：`packages/observability/src/logger.ts`、`packages/observability/src/logger.test.ts`、`packages/observability/src/index.ts`
- 技术决策：注入 destination 时强制 JSON（pino 的 transport 与 destination 互斥）；脱敏按字段名路径匹配，同时在 JSDoc 中明确"完整模型输入不应进入日志"的调用方责任。
- 验证方式：`cd packages/observability && pnpm typecheck && pnpm test`
- 验证结果：typecheck 通过；7 个测试通过（JSON 输出、级别过滤、嵌套 apiKey/password/connectionString 脱敏、非法级别报错、LOG_LEVEL 生效）。
- 遗留问题：无

### 任务 5：五个占位包的职责契约（agent-core / model-providers / tools / sandbox / evals）

- 状态：完成
- 完成内容：每个包含 package.json/tsconfig.json/src/index.ts/typecheck/test 脚本，导出最小但真实的契约而非空壳：
  - agent-core：11 状态转换表 + `canTransition()` + 各阶段工具策略（analyzing 只读、planning 无工具）。
  - model-providers：`ModelProvider` 接口 + `parseModelIdentifier("provider/model")`。
  - tools：7 个受控工具名 + 输出上限 + `ControlledTool` 接口，明确无 `exec` 工具。
  - sandbox：默认沙箱限制（断网/非 root/只读根 FS/禁止挂载 Docker Socket 与宿主凭据）+ 命令 Allowlist 校验。
  - evals：7 个指标口径 + `EvalCase` 契约 + `computeStablePassRate()`（连续 3 次全过才算稳定）。
- 关键文件：各包 `src/index.ts` 与 `src/index.test.ts`
- 技术决策：占位包不做虚假实现，只固化项目说明第 7/8/9/14 节已确定的设计约束，后续实现直接以此为起点。
- 验证方式：`pnpm -r --if-present typecheck && pnpm -r --if-present test`
- 验证结果：8 个 workspace 包 typecheck 全部通过；agent-core 8、model-providers 7、tools 5、sandbox 6、evals 5 个测试全部通过。
- 遗留问题：无

### 任务 6：API 服务（Fastify 5 健康检查）

- 状态：完成
- 完成内容：`buildApp()` 工厂（不监听端口，测试用 inject）；`GET /health` 仅进程存活；`GET /ready` 并行探测 PostgreSQL（SELECT 1）与 Redis（PING），单项 2s 超时，失败返回 503 + `{error:{code,message,checks[]}}` 结构化错误；`GET /api/version` 返回名称/版本/阶段；Zod 校验环境变量（BaseEnv + API_PORT coerce）；`server.ts` 入口带 SIGTERM/SIGINT 优雅关闭；tsup 打包为可运行 ESM 产物。
- 关键文件：`apps/api/src/app.ts`、`apps/api/src/env.ts`、`apps/api/src/readiness.ts`、`apps/api/src/server.ts`、`apps/api/src/app.test.ts`、`apps/api/tsup.config.ts`
- 技术决策：
  1. 依赖客户端惰性创建：pg.Pool 首查询才连接；ioredis 用 lazyConnect，仅在 /ready 探测时连接（并在探测前处理 wait/close 状态先 connect 再 ping，修复了 lazyConnect+enableOfflineQueue:false 时 Redis 在线仍误报 down 的缺陷）。
  2. workspace 内部包通过 tsup `noExternal: [/^@patchflow\//]` 打进 bundle——否则 Node 运行时无法解析源码包内的 .js 说明符。
  3. pino-pretty 显式加入 apps/api 依赖：pnpm 严格 node_modules 下 transport 目标必须能从使用方解析。
  4. `loggerInstance` 类型用 FastifyBaseLogger，规避 exactOptionalPropertyTypes 下 pino Logger 与 Fastify 泛型的不兼容。
- 验证方式：
  - `cd apps/api && pnpm typecheck && pnpm test && pnpm build`
  - 真实冒烟：`DATABASE_URL=... REDIS_URL=... API_PORT=3001 node dist/server.js` 后 `curl /health`、`/api/version`、`/ready`
- 验证结果：typecheck 通过；7 个注入测试通过（health、version、ready 成功、Redis 失败 503、PG 失败 503、超时 503）；真实进程下 `/health` → `{"status":"ok"}`，`/api/version` → `{"stage":"Foundation"}`，依赖未启动时 `/ready` → HTTP 503 结构化错误（postgres: role 不存在、redis: 未连接），符合预期。
- 遗留问题：依赖正常时的 `/ready` 200 待 Docker Compose 启动后复验（见任务 9）。

### 任务 7：Worker 队列骨架（BullMQ）

- 状态：完成（单元测试完成；真实队列冒烟待 Redis 就绪，见任务 9）
- 完成内容：`run.execute` Job Schema（runId 必填 UUID + 可选 enqueuedAt）；`RunQueue` 封装（attempts=3、指数退避）；Worker 处理器只校验消息 + 输出结构化日志（含 runId/jobName），不实现 Agent 执行；非法数据抛 `InvalidJobDataError` 由 BullMQ 标记 failed；入口带 SIGTERM/SIGINT 优雅关闭（先停消费再关队列）；队列连接/Worker 创建全部在显式函数中，模块导入零副作用；`scripts/worker-smoke.ts` 冒烟脚本（投递任务 → 等待消费 → 优雅退出）。
- 关键文件：`apps/worker/src/job.ts`、`apps/worker/src/queue.ts`、`apps/worker/src/worker.ts`、`apps/worker/src/connection.ts`、`apps/worker/src/env.ts`、`apps/worker/src/index.ts`、`apps/worker/src/worker.test.ts`、`apps/worker/scripts/worker-smoke.ts`
- 技术决策：
  1. BullMQ 连接用 `maxRetriesPerRequest: null`（BullMQ 硬性要求）+ lazyConnect，由入口显式 `connect()`，连接失败立即退出而非静默重试。
  2. 处理器为纯函数 `processRunExecuteJob(data, logger)`，单元测试不需要 Redis。
  3. Worker 环境变量只要求 REDIS_URL（omit DATABASE_URL），避免 Foundation 阶段无谓强依赖。
  4. pnpm 11 的 allowBuilds 机制批准了 `msgpackr-extract`（BullMQ 原生解析组件）的安装脚本。
- 验证方式：`cd apps/worker && pnpm typecheck && pnpm test && pnpm build`
- 验证结果：typecheck 通过；12 个测试通过（Schema 合法/非法各 5+、解析错误信息、处理器返回 acknowledged 与结构化日志、null/undefined 拒绝）；tsup 打包成功。
- 遗留问题：`pnpm smoke`（真实 Redis 队列闭环）待任务 9 Docker Compose 启动 Redis 后执行。

### 任务 8：Web 基础页面（Next.js 16 + React 19）

- 状态：完成
- 完成内容：Foundation 首页——PatchFlow 名称 + 一句话定位 + 阶段徽章；API/PostgreSQL/Redis/Worker 四张状态卡（API 实时探测，其余为明确标注的占位）；服务端 `fetchApiHealth()` 封装（3s 超时、非 2xx、响应格式异常、连接失败全部折叠为结构化 down 状态）；API 不可用时页面显示错误横幅而非白屏。
- 关键文件：`apps/web/app/page.tsx`、`apps/web/app/layout.tsx`、`apps/web/app/globals.css`、`apps/web/src/lib/api.ts`、`apps/web/src/lib/api.test.ts`、`apps/web/next.config.ts`、`apps/web/tsconfig.json`
- 技术决策：
  1. 不引入 Tailwind，直接用约 100 行原生 CSS 满足"简洁清晰"要求，减少 Foundation 阶段依赖面。
  2. API 探测放在服务端组件（force-dynamic），浏览器不直接跨域请求 API；`NEXT_PUBLIC_API_BASE_URL` 服务端读取，默认 `http://localhost:3001`。
  3. web tsconfig 覆盖 base 的 module/jsx 设置（ESNext + Bundler + preserve），关闭 verbatimModuleSyntax 以适配 Next 编译管线。
- 验证方式：
  - `cd apps/web && pnpm typecheck && pnpm test && pnpm build`
  - 冒烟：`npx next start -p 3000` + `curl -s localhost:3000`，分别验证 API 关闭与开启两种情况
- 验证结果：typecheck 通过；6 个测试通过（默认地址/环境变量覆盖、ok、503、格式异常、连接失败）；`next build` 成功（Route / 为 Dynamic SSR）；API 关闭时页面含 "无法连接 API：fetch failed" 横幅且 Foundation 徽章正常渲染；API 开启时 API 卡片显示 "存活（GET http://localhost:3001/health → 200）/ 正常"。
- 遗留问题：无

### 任务 9：Docker Compose（PostgreSQL + Redis）与真实依赖联调

- 状态：完成
- 完成内容：Compose 编排 postgres:18-alpine + redis:8-alpine（命名 Volume、healthcheck、restart 策略、AOF 持久化、禁用 FLUSHALL）；端口/账号/密码/库名全部支持 `.env` 覆盖。完成真实链路联调：migration 落库、API /ready 全绿、Worker 队列消费闭环、SIGTERM 优雅退出。
- 关键文件：`docker-compose.yml`、`.env.example`（复制为 `.env`）
- 技术决策：
  1. **宿主端口默认 5433/6380**：本机 5432/6379 已被用户自己的 PostgreSQL/Redis 占用（lsof 确认），默认避开冲突，需要时用 POSTGRES_PORT/REDIS_PORT 改回。
  2. **PG18 挂载点改为 `/var/lib/postgresql`**：postgres:18 镜像要求挂父目录（否则容器崩溃循环，官方 issue #37），首次启动时发现并通过 `docker logs` 定位。
  3. Redis 启用 appendonly（BullMQ 丢失恢复语义）并重命名禁用 FLUSHALL。
  4. 未把 Web/API/Worker 做成生产镜像（按本次范围要求）。
- 验证方式：
  - `cp .env.example .env && docker compose up -d`，轮询 `docker inspect` health 状态
  - `pnpm db:migrate` + `docker exec patchflow-postgres psql -U patchflow -d patchflow -c '\dt' -c '\d run_events'`
  - `node dist/server.js`（API）后 `curl /ready`；`REDIS_URL=redis://localhost:6380 pnpm smoke`（worker 包）；`kill -TERM` 真实 Worker 进程
- 验证结果：
  - 两容器 healthy（postgres 5433→5432、redis 6380→6379）。
  - migration 成功；`runs`、`run_events` 两表存在，`run_events_run_id_sequence_key UNIQUE(run_id,sequence)`、`ON DELETE CASCADE`、`run_events_run_id_idx` 均确认。
  - `/ready` 依赖正常时 HTTP 200：`{"status":"ready","checks":[{postgres,up,18ms},{redis,up,10ms}]}`（与之前依赖不可用时的 503 结构化错误形成对照）。
  - Worker 冒烟：任务投递 → 消费（结构化日志含 runId/jobName）→ 优雅关闭 → 退出码 0；真实入口 `node dist/index.js` 收到 SIGTERM 后输出 "Worker 已优雅退出" 并干净退出。
- 遗留问题：无

### 任务 10：CI、README 与架构文档

- 状态：完成
- 完成内容：GitHub Actions（PR + main push 触发；pnpm/action-setup + Node 24 + lockfile 安装，依次执行 format:check / lint / typecheck / test / build，不依赖真实模型 API 与数据库）；README（项目介绍、Foundation 已实现范围、技术栈、前置要求、从克隆到运行命令、环境变量表、各服务启动方法、质量命令、目录说明、非目标与下一步）；docs/architecture.md（组件关系图、"PostgreSQL 是事实来源 / BullMQ 只投递"、SSE vs WebSocket、为什么不用 LangChain/LangGraph、Foundation 已落地设计与待实现决策）。
- 关键文件：`.github/workflows/ci.yml`、`README.md`、`docs/architecture.md`
- 技术决策：CI 用 pnpm/action-setup 而非 corepack（Node 25 起不再内置 corepack，且 CI 固定 Node 24 时代码库已记录 packageManager 版本）；并发组取消同分支重复运行；README 明确列出"尚未实现"清单，不夸大当前能力。
- 验证方式：`pnpm format:check`（校验文档格式）；workflow YAML 语法由本地 `docker compose config` 之外的 `ruby -ryaml` 思路不可用，改用 GitHub Actions 实际触发——本仓库尚未关联远程，CI 首次真实执行待推送后验证。
- 验证结果：README / architecture / ci.yml 均通过 `pnpm format:check`；CI 步骤与本地通过的五条命令一一对应，本地全绿即 CI 预期全绿。
- 遗留问题：CI 尚未在 GitHub 上真实运行（无远程仓库，按约定不创建远程、不 push）；推送后需要观察一次 Actions 结果。

### 任务 11：全量质量检查与修复

- 状态：完成
- 完成内容：`pnpm format`（格式化 16 个文件）后依次跑 format:check / lint / typecheck / test / build，并修复过程中暴露的问题：ESLint `no-misused-promises` 非法选项改为 `checksVoidReturn: false`；14 个 lint 错误（未用导入/变量、require-await、any 断言、unsafe member access）全部按根因修复而非禁用规则；确认 `.env` 被忽略、Git 状态无敏感文件。
- 关键文件：`eslint.config.mjs`、`apps/api/src/app.test.ts`、`apps/api/src/app.ts`、`apps/worker/src/{worker,env}.ts`、`apps/worker/scripts/worker-smoke.ts`、`packages/db/src/schema.ts`
- 技术决策：`processRunExecuteJob` 保留 async 并以带注释的 eslint-disable 声明原因（后续将 await 状态机），其余 14 处全部真实修复；测试中的 `any` 断言改为 `response.json<T>()` 泛型。
- 验证方式（与验收清单一一对应）：
  ```bash
  pnpm format:check   # All matched files use Prettier code style!
  pnpm lint           # eslint . 退出码 0
  pnpm typecheck      # 11 个包 Done
  pnpm test           # 11 个包 116 个用例全部通过
  pnpm build          # api/worker tsup + web next build 成功
  ```
- 验证结果：全部通过。测试分布：contracts 40、api 7、worker 12、web 6、db 13、observability 7、agent-core 8、model-providers 7、tools 5、sandbox 6、evals 5。
- 遗留问题：无

## 今日总结

- 完成了什么：PatchFlow Foundation 阶段全部完成——pnpm Monorepo（TS 5.9 strict、ESLint 10 类型感知、Prettier、Vitest、catalog 版本统一）、11 个 workspace 包（3 个应用 + 8 个包，含 5 个带真实契约的占位包）、contracts 全套 Zod 契约、Drizzle Schema + 首份 migration（已在真实 PostgreSQL 应用）、Fastify 健康检查（/health、/ready 含 503 结构化错误）、BullMQ Worker 骨架（含优雅关闭与真实队列冒烟）、Next.js Foundation 页面（API 探测 + 错误兜底）、Docker Compose（PG18 + Redis8 均 healthy）、CI/README/架构文档。全量质量门 116 测试全绿。
- 当前风险：
  1. CI 未在 GitHub 真实执行过（无远程仓库）。
  2. 本地 Node 为 v25.7.0 而非 24 LTS（无版本管理器），engines 约束已兼容，但与 CI 环境存在差异。
  3. Compose 默认宿主端口 5433/6380（因本机 5432/6379 被占用），换机器开发时需要注意或改回。
  4. db 包尚无真实数据库集成测试（当前是结构测试 + 手工 migration 验证），后续引入 Testcontainers。
- 下一步：实现最小 CLI 闭环——导入本地 Git 仓库 → 创建 Worktree → 跑通 read_file / search_code / apply_patch 三个受控工具 → 产出可查看 Diff（先不接模型，验证工具与隔离边界）。
