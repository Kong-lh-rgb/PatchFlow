# PatchFlow 项目说明

> 面向 GitHub Issue 和代码缺陷的可验证 AI Coding Agent：在隔离环境中理解仓库、复现问题、生成补丁、运行测试，并交付带证据的修改结果。

## 1. 项目定位

PatchFlow 不是一个“让模型随意执行 Shell”的聊天机器人，也不是把 Planner、Coder、Reviewer 拆成几个角色的多 Agent 演示。它要解决的是一个具体问题：

**给定代码仓库、Issue 和验收条件，系统能否在安全、可追踪、可恢复的前提下生成一个经过测试验证的 Patch？**

它与 Vesta 的关系是互补的：

- Vesta 展示通用 Agent Runtime/Harness 能力，包括上下文、记忆、恢复、审批和工具治理。
- PatchFlow 展示如何把 Agent 工程能力落到真实的软件开发工作流，并用客观指标验证效果。

一句话介绍：

> PatchFlow 是一个使用 TypeScript 构建的代码维护 Agent。用户输入仓库和 Issue 后，它会在独立 Git Worktree 与 Docker 沙箱中分析代码、复现问题、生成修改、执行测试，并输出 Patch、测试证据和完整执行轨迹；Commit、Push、创建 PR 等外部副作用必须由用户审批。

## 2. 为什么值得做

普通 Coding Demo 往往只展示“模型写出了一段代码”，但无法回答下面的问题：

- 修改是否真正解决了 Issue？
- 是否破坏了仓库原有功能？
- 模型执行过哪些命令、读取过哪些文件？
- 工具失败、进程重启或模型输出异常后能否继续？
- 模型是否越权访问文件、联网或执行危险命令？
- 一次成功是偶然，还是多次运行都能稳定成功？

PatchFlow 的重点不是生成代码本身，而是围绕代码修改建立一条**可执行、可验证、可审计、可评测**的工程链路。

## 3. 项目目标与非目标

### 3.1 MVP 目标

第一版完成以下闭环：

1. 导入本地 Git 仓库或公开 GitHub 仓库。
2. 输入 Issue、目标分支和可选验收说明。
3. 为每个 Run 创建独立 Git Worktree。
4. Agent 使用受控工具分析代码并尝试复现问题。
5. Agent 生成 Patch，并进入“修改—测试—修复”有限循环。
6. 保存模型请求、工具调用、状态变化、测试结果和最终 Diff。
7. 页面实时展示执行进度，支持取消和失败恢复。
8. 输出 Patch、修改说明、测试证据和风险提示。
9. Commit、Push、创建 PR 等动作进入人工审批。
10. 建立一套可重复执行的代码修复评测集。

### 3.2 第一版明确不做

- 不做可处理所有语言和所有仓库的通用 Coding Agent。
- 不做多个角色互相对话的形式化多 Agent 系统。
- 不把任意 Shell 直接暴露给模型。
- 不默认允许网络访问、安装依赖或修改仓库外文件。
- 不自动 Commit、Push 或创建 PR。
- 不在 MVP 引入向量数据库和全仓库代码 Embedding。
- 不追求自动完成大型需求，只处理边界清晰、可测试的 Bug 和小型改动。

## 4. 目标用户与核心场景

目标用户是需要处理重复性 Issue、修复小型缺陷或验证 AI Coding Agent 能力的开发者。

核心场景：

1. 开发者选择一个仓库并提交 Issue。
2. 系统检查仓库、基础分支、依赖和测试入口。
3. Agent 搜索相关代码，建立问题假设。
4. Agent 运行复现命令，获取失败证据。
5. Agent 制订简短修改计划并编辑代码。
6. Agent 运行目标测试、相关测试、类型检查和构建。
7. 如果失败，Agent 根据错误继续修改，但受最大轮数和预算限制。
8. 验证通过后，系统展示 Patch 和证据报告。
9. 用户决定下载 Patch、批准 Commit，或拒绝结果。

## 5. 技术选型

项目业务代码统一使用 TypeScript，开启 `strict`，避免同时维护 Python 与 TypeScript 两套运行时。

| 层次       | 选择                                         | 用途与原因                                                |
| ---------- | -------------------------------------------- | --------------------------------------------------------- |
| Runtime    | Node.js 24 LTS                               | 使用生产 LTS 版本；原生支持现代 Web API、流和子进程控制   |
| 语言       | TypeScript 5.x，strict                       | 前后端、Worker、Agent Core、评测共享类型                  |
| 包管理     | pnpm workspace                               | 轻量 Monorepo，便于共享 contracts、db 和 agent-core       |
| Web        | Next.js 16 + React 19                        | Run 列表、执行详情、Diff、审批和评测看板                  |
| API        | Fastify 5                                    | 类型友好、插件边界清晰，适合独立控制面 API                |
| 实时反馈   | SSE + REST                                   | 服务端只需单向推送进度；审批、取消等命令继续使用 REST     |
| 后台任务   | BullMQ + Redis                               | 分发长任务、控制 Worker 并发、处理进程重启后的重新投递    |
| 持久化     | PostgreSQL + Drizzle ORM                     | 保存 Run 状态、事件、工具调用、审批、Artifact 与评测结果  |
| 数据校验   | Zod                                          | API、队列消息、模型工具参数共享同一套 Schema              |
| 模型接入   | 官方 OpenAI/Anthropic SDK + Provider Adapter | 避免被框架状态模型绑定，明确掌控 Tool Calling 循环        |
| GitHub     | Git CLI + Octokit                            | Git 负责本地仓库操作，Octokit 负责可选 GitHub App/PR 操作 |
| 代码检索   | ripgrep + 文件读取工具                       | MVP 先验证 Agent 链路；不提前引入代码 RAG                 |
| 隔离执行   | Git Worktree + Docker                        | 每个 Run 独立工作区；命令在受限容器中执行                 |
| 日志与追踪 | Pino + OpenTelemetry                         | 结构化日志、耗时、Token 和错误观测                        |
| 测试       | Vitest + Testcontainers + Playwright         | 单元、真实依赖集成和端到端页面测试                        |
| CI         | GitHub Actions                               | 类型检查、测试、构建和镜像验证                            |
| 本地环境   | Docker Compose                               | 启动 PostgreSQL、Redis、API、Worker 和 Web                |

### 5.1 为什么不用 LangChain/LangGraph

PatchFlow 的核心竞争力是对执行边界、状态转换、恢复语义和工具权限的掌控。MVP 直接实现一个有限状态工作流更容易解释和调试：

- 每个状态为什么产生一目了然。
- 模型输入和工具输出可以完整记录。
- 恢复、重试和审批语义由项目自己定义。
- 面试时能讲清底层机制，而不是只会调用框架接口。

这不代表这些框架没有价值，而是第一版不需要用它们解决当前问题。

### 5.2 为什么用 SSE 而不是 WebSocket

运行过程中主要是服务端向页面推送状态、日志和测试结果，交互方向基本是单向的。SSE 自带断线重连和 `Last-Event-ID`，实现和排障成本更低。用户发起 Run、取消和审批仍使用普通 HTTP API。

### 5.3 为什么 PostgreSQL 和 BullMQ 都要有

- PostgreSQL 是业务状态的唯一事实来源。
- BullMQ 只负责把待执行任务送到 Worker，不代表任务真实完成状态。
- 队列采用至少一次投递，Worker 必须通过 Run 状态和幂等键避免重复执行。
- 即使 Redis 丢失临时任务，系统也能从 PostgreSQL 找到未完成 Run 并重新入队。

## 6. 总体架构

```text
Browser
  │ REST commands / SSE events
  ▼
Fastify API ─────────────── PostgreSQL
  │ create job                 ▲
  ▼                            │ checkpoints / events
BullMQ + Redis                 │
  │                            │
  ▼                            │
TypeScript Worker ─────── Agent Core
  │                         │ tool registry
  │                         ▼
  ├── Git Worktree ─── Docker Sandbox
  ├── Model Provider Adapter
  └── Artifact Store (local MVP, object storage later)
```

模块职责：

- Web：展示任务、实时轨迹、Diff、验证结果和审批操作。
- API：鉴权、参数校验、创建 Run、状态查询和审批入口。
- Worker：消费任务、持有执行租约、驱动 Agent 状态机。
- Agent Core：上下文构建、模型请求、工具调度、预算和停止条件。
- Sandbox：创建 Worktree、运行容器、限制命令和资源。
- Eval Runner：批量运行固定 Case，计算通过率和稳定性。

## 7. Run 状态机

```text
queued
  → preparing
  → analyzing
  → reproducing
  → planning
  → editing
  → verifying ──失败且预算充足──→ editing
  → awaiting_approval（存在外部副作用时）
  → completed

任意执行态 → failed / cancelled
```

状态语义：

| 状态                | 含义                                       |
| ------------------- | ------------------------------------------ |
| `queued`            | 已创建 Run，等待 Worker                    |
| `preparing`         | 创建 Worktree、读取配置、启动沙箱          |
| `analyzing`         | 搜索相关文件并形成问题假设                 |
| `reproducing`       | 尝试让原问题稳定失败，保存失败证据         |
| `planning`          | 输出有限、可验证的修改计划                 |
| `editing`           | 调用受控编辑工具修改 Worktree              |
| `verifying`         | 运行目标测试、回归测试、类型检查和构建     |
| `awaiting_approval` | 等待用户批准 Commit、Push、PR 或高风险改动 |
| `completed`         | 已输出 Patch 和验证报告                    |
| `failed`            | 达到重试、预算或不可恢复错误边界           |
| `cancelled`         | 用户取消，Worker 完成安全清理              |

状态转换必须经过统一函数校验并使用数据库事务，禁止业务模块随意更新字符串状态。

## 8. Agent 执行设计

### 8.1 单 Agent、分阶段工作流

第一版使用一个 Agent，由代码控制阶段，不把 Planner、Coder、Reviewer伪装成三个独立 Agent。每个阶段有不同的工具权限和输出 Schema：

1. `analyzing`：只允许搜索和读取。
2. `reproducing`：允许执行已配置的测试命令。
3. `planning`：模型输出结构化修改计划，不执行工具。
4. `editing`：允许对工作区内文件应用 Patch。
5. `verifying`：只允许执行验证命令和读取错误相关文件。
6. `delivery`：生成最终报告，不继续修改代码。

### 8.2 工具集合

MVP 只向模型暴露以下工具：

| 工具            | 作用                        | 关键限制                            |
| --------------- | --------------------------- | ----------------------------------- |
| `list_files`    | 枚举目录和匹配文件          | 只能访问仓库根目录                  |
| `search_code`   | 使用 ripgrep 搜索符号和文本 | 限制结果条数和总字符数              |
| `read_file`     | 按行读取文件                | 限制单次行数；二进制文件拒绝        |
| `apply_patch`   | 应用结构化补丁              | 路径规范化；禁止越过 Worktree       |
| `run_tests`     | 运行允许的测试配置          | 不接受任意 Shell 字符串             |
| `run_typecheck` | 执行类型检查                | 命令来自可信项目配置                |
| `git_diff`      | 获取当前修改                | 大 Diff 保存为 Artifact，只返回摘要 |

模型不能直接获得 `exec(command: string)`。命令使用 `{ program, args[] }` 形式传递给 `spawn`，不开启 Shell。

### 8.3 上下文组成

每次模型请求只构建当前阶段需要的信息：

- 原始 Issue 和用户验收条件。
- 仓库基本信息、目标分支和允许修改的路径。
- 当前阶段、计划、预算和停止条件。
- 已确认的关键事实与最近工具结果。
- 相关文件片段，而不是整个仓库内容。
- 当前 Diff 摘要与最近一次验证失败。

超长工具结果写入 Artifact，模型上下文中仅保留摘要和引用。仓库代码、Issue 评论和 README 都按不可信输入处理，不允许其中内容修改系统权限或审批规则。

### 8.4 Checkpoint 边界

至少在以下边界保存状态：

1. 模型请求前：保存当前阶段、上下文版本和预算。
2. 工具执行前：保存 Tool Call ID、名称、参数摘要和 `started` 状态。
3. 工具返回后：保存结果、Artifact、耗时和 `succeeded/failed` 状态。
4. 状态转换后：写入新状态和递增版本号。

进程中断后，系统不能假设 `started` 的副作用工具既没执行也已完成：

- 读取、搜索类工具可以安全重试。
- 测试类工具可以在新容器中重试。
- 编辑工具先检查 Patch 是否已应用，再决定跳过或重试。
- Commit、Push、PR 依赖幂等键和外部状态查询，且必须经过审批。

### 8.5 预算与停止条件

每个 Run 配置：

- 最大模型调用次数。
- 最大工具调用次数。
- 最大修改—验证循环次数。
- 最大输入/输出 Token。
- 最大墙钟时间。
- 单个命令超时时间。
- 最大修改文件数和 Diff 行数。

达到任一限制后停止自动执行，输出当前 Patch、失败证据和停止原因，不让 Agent 无限自我修复。

## 9. 沙箱与安全边界

每个 Run 创建独立 Git Worktree，再挂载到临时 Docker 容器。建议默认配置：

- 容器非 root 用户。
- 默认 `--network none`。
- 根文件系统只读，Worktree 目录可写。
- `/tmp` 使用限额 tmpfs。
- 限制 CPU、内存、PID 数和执行时间。
- 禁止挂载 Docker Socket、SSH Key、用户主目录和宿主机凭据。
- 只向容器注入运行测试必需的环境变量。
- 命令必须命中仓库配置中的 Allowlist。
- 对 `.github/workflows`、依赖锁文件、部署脚本和权限文件的修改标记为高风险。

需要审批的动作：

- 安装或更新依赖。
- 修改 CI、部署、安全策略或数据库迁移。
- Commit、Push、创建/更新 PR。
- 开启网络访问。
- 扩大允许修改路径和执行命令范围。

审批通过后必须重新检查当前 Run、Worktree HEAD、Diff Hash 和审批版本，避免审批期间状态变化后执行过期动作。

## 10. 数据模型

核心表建议如下：

| 表              | 关键字段                                           | 用途                  |
| --------------- | -------------------------------------------------- | --------------------- |
| `repositories`  | id, source, url/path, default_branch, config       | 仓库配置              |
| `tasks`         | id, repository_id, title, issue_body, acceptance   | 用户目标              |
| `runs`          | id, task_id, status, phase, version, budget, lease | 一次执行及恢复状态    |
| `run_steps`     | id, run_id, phase, sequence, model_usage, status   | 每次模型决策          |
| `tool_calls`    | id, step_id, name, args, status, result_ref        | 工具执行记录          |
| `run_events`    | id, run_id, sequence, type, payload                | SSE 与审计事件流      |
| `approvals`     | id, run_id, action, payload_hash, status           | 人工审批              |
| `artifacts`     | id, run_id, kind, path, sha256, size               | Patch、日志、测试报告 |
| `eval_cases`    | id, repo_ref, issue, hidden_checks                 | 固定评测样本          |
| `eval_attempts` | id, case_id, run_id, seed, metrics                 | 多次评测结果          |

关键约束：

- `runs.version` 用于乐观并发控制。
- 每个 Worker 通过 `lease_owner` 和 `lease_expires_at` 持有短租约并续期。
- `run_events` 在单个 Run 内使用单调递增 `sequence`，支持 SSE 断点续传。
- Artifact 保存 SHA-256，确保报告引用的内容未发生变化。

## 11. API 草案

```text
POST   /api/repositories
GET    /api/repositories/:id

POST   /api/tasks
GET    /api/tasks/:id

POST   /api/runs
GET    /api/runs/:id
GET    /api/runs/:id/events          # SSE
POST   /api/runs/:id/cancel
POST   /api/runs/:id/retry

GET    /api/runs/:id/diff
GET    /api/runs/:id/artifacts
GET    /api/artifacts/:id

POST   /api/approvals/:id/resolve

POST   /api/evals
GET    /api/evals/:id
```

`POST /api/runs` 示例：

```json
{
  "taskId": "task_123",
  "baseRef": "main",
  "model": "provider/model-name",
  "limits": {
    "maxIterations": 6,
    "maxToolCalls": 40,
    "timeoutSeconds": 1200
  }
}
```

SSE 事件至少包含：

- `run.status_changed`
- `step.started`
- `step.completed`
- `tool.started`
- `tool.completed`
- `verification.completed`
- `approval.requested`
- `artifact.created`
- `run.completed`
- `run.failed`

## 12. 前端页面

### 12.1 仓库与任务页

- 导入仓库。
- 展示默认分支、检测到的语言与测试配置。
- 创建 Issue 任务和验收条件。

### 12.2 Run 执行详情页

- 当前状态和阶段时间线。
- 模型 Step 与 Tool Call 轨迹。
- 实时测试日志。
- Token、耗时和工具次数预算。
- 取消、重试和审批操作。

### 12.3 结果页

- 文件级 Diff。
- 复现前失败证据和修改后通过证据。
- 修改说明、风险、未完成项。
- 下载 `.patch`。
- 批准 Commit/PR（后续阶段实现）。

### 12.4 Eval 看板

- 样本数、解决率、回归安全率和稳定通过率。
- 每个 Case 的三次运行结果。
- 失败原因分布、平均时延、Token 和工具调用数。

## 13. Monorepo 目录结构

```text
patchflow/
├── apps/
│   ├── web/                 # Next.js
│   ├── api/                 # Fastify control plane
│   └── worker/              # BullMQ consumer
├── packages/
│   ├── agent-core/          # 状态机、上下文、预算、模型循环
│   ├── model-providers/     # OpenAI/Anthropic adapter
│   ├── tools/               # 搜索、读取、编辑、测试、Diff
│   ├── sandbox/             # Worktree、Docker、命令策略
│   ├── contracts/           # Zod schemas 和共享类型
│   ├── db/                  # Drizzle schema 与 migrations
│   ├── observability/       # 日志、trace、metrics
│   └── evals/               # 数据集、runner、metrics
├── fixtures/                # 可复现的评测仓库/Case
├── docker/
├── docs/
├── pnpm-workspace.yaml
├── tsconfig.base.json
└── docker-compose.yml
```

## 14. 评测方案

### 14.1 数据集

第一阶段准备 10 个 Case，稳定后扩展到 20～30 个。Case 来源：

- 自己项目历史中已经修复过的 Bug。
- 小型开源项目的历史 Issue 与修复前 Commit。
- 人工设计但具有真实测试边界的缺陷。

每个 Case 固定：

- 修复前仓库 Commit。
- Issue 描述。
- Agent 可见测试和命令。
- 评测器独占的隐藏测试。
- 允许修改路径与禁止动作。
- 最大预算。

不能把原始修复 Commit、隐藏测试或标准答案提供给 Agent。

### 14.2 指标

| 指标                  | 定义                                                |
| --------------------- | --------------------------------------------------- |
| Issue Resolution Rate | 隐藏验收测试通过的 Case / 全部 Case                 |
| Regression-Safe Rate  | 修复后原有回归测试仍通过的 Case / 已解决 Case       |
| Stable Pass Rate      | 同一 Case 连续运行 3 次均通过 / 全部 Case           |
| Patch Apply Rate      | 输出 Patch 能干净应用 / 全部 Run                    |
| Unsafe Action Rate    | 发生越权文件、命令、网络或未审批外部动作 / 全部 Run |
| Recovery Success Rate | 注入中断后能正确恢复并结束 / 中断实验数             |
| Efficiency            | 每个成功 Case 的时延、Token、模型调用和工具调用数   |

一次通过只能说明该次结果正确；连续三次通过才反映在模型随机性和工具不稳定存在时的稳定性，二者不能合并为同一个指标。

### 14.3 对照实验

至少比较两套方案：

- Baseline：模型拥有同样工具，但只有普通循环，无阶段状态、复现门槛和验证策略。
- PatchFlow：分阶段工作流、结构化工具、预算、验证与恢复机制全部开启。

固定模型、温度、仓库 Commit 和预算，记录差异。项目最终不只展示 Demo，而是展示一份可以复现的评测报告。

## 15. 六周开发计划

### 第 1 周：最小闭环

- 初始化 pnpm Monorepo、TypeScript strict、lint、test 和 CI。
- 实现仓库导入、Worktree 创建、`read_file/search_code/apply_patch`。
- 完成单个 Issue 从分析到生成 Patch 的 CLI 路径。

交付：一个本地 Case 能生成可查看的 Diff。

### 第 2 周：验证与修复循环

- 实现 Docker Sandbox 和允许命令配置。
- 实现 `run_tests/run_typecheck/git_diff`。
- 加入最大轮数、Token、工具次数和超时预算。
- 形成“复现—修改—验证—再修改”闭环。

交付：至少 3 个 Case 可自动验证。

### 第 3 周：持久化与恢复

- PostgreSQL 数据模型和 Drizzle migration。
- BullMQ Worker、Run lease、幂等任务处理。
- 保存 Step、Tool Call、Event 和 Artifact。
- 做进程中断注入，验证恢复行为。

交付：Worker 被杀死后可恢复未完成 Run。

### 第 4 周：Web、实时反馈与审批

- Next.js 页面与 Fastify API。
- SSE 实时事件流和断线续传。
- Run 时间线、工具详情、测试日志和 Diff 页面。
- 依赖、敏感文件和外部操作审批。

交付：完整可演示的 Web 工作流。

### 第 5 周：评测系统

- 建立 10 个固定 Eval Case。
- 实现隐藏测试、三次重复运行和指标计算。
- 加入 Baseline 对照实验。
- 汇总失败类型和成本数据。

交付：第一份可复现评测报告。

### 第 6 周：工程收尾

- Docker Compose 一键启动。
- 补齐单元、集成和端到端测试。
- README、架构图、威胁模型和演示视频。
- 根据评测结果只优化最主要的失败类型。

交付：可放进简历、可现场演示、可解释数据的项目。

## 16. MVP 验收标准

第一版完成的判断标准：

- 10 个固定 Case 能一键批量运行。
- 至少 6 个 Case 通过隐藏验收测试；未达到时也必须给出真实报告，不修改口径。
- 所有 Run 都能看到完整状态、模型 Step、工具调用、Artifact 和停止原因。
- 通过的 Case 必须同时满足目标测试与回归测试要求。
- 任意命令都在容器中运行，模型不能调用任意 Shell。
- 未经批准不会 Commit、Push、创建 PR 或开启网络。
- Worker 中断后能够恢复或明确标记不可恢复，不能静默重复副作用。
- 同一 Case 可重复运行三次并计算 Stable Pass Rate。
- 新用户根据 README 能在本地启动并完成一个 Demo。

## 17. 项目展示与简历表达

### 17.1 面试时的 60 秒介绍

> PatchFlow 是我用 TypeScript 做的一个可验证代码修复 Agent。用户给出 Git 仓库和 Issue 后，它会先搜索相关代码并尝试复现问题，再在独立 Worktree 和 Docker 沙箱中修改代码、运行测试，最终交付 Patch 和完整验证证据。我没有采用简单的无限 Tool Calling 循环，而是把执行过程建模成有预算的状态机，同时对工具权限、Checkpoint、失败恢复和外部操作审批做了明确设计。为了避免项目只停留在 Demo，我还建立了固定 Issue 数据集，用隐藏测试、回归安全率和连续三次稳定通过率评估效果。

### 17.2 简历描述模板

只有功能和数据真实完成后再填写数字：

- 使用 TypeScript、Fastify、Next.js 与 PostgreSQL 构建面向 GitHub Issue 的可验证 Coding Agent，实现代码检索、问题复现、补丁生成、测试验证和结果交付闭环。
- 基于 Git Worktree 与 Docker 构建按 Run 隔离的执行沙箱，通过命令 Allowlist、资源限制、默认断网和人工审批控制代码执行风险。
- 设计可恢复的状态机与持久化事件轨迹，记录模型 Step、工具调用、测试证据和 Artifact，支持 Worker 中断后的幂等恢复。
- 建立 **[N]** 个固定 Issue 的评测集，通过隐藏测试和 3 次重复运行评估解决率 **[X%]**、回归安全率 **[Y%]** 与稳定通过率 **[Z%]**。

不要在尚未完成评测前编造提升比例，也不要把“跑通一次”写成“稳定达到”。

## 18. 风险与范围控制

| 风险                      | 控制方式                                                             |
| ------------------------- | -------------------------------------------------------------------- |
| 项目范围过大              | MVP 只支持 TS/Python 小型仓库和可测试 Bug                            |
| 模型反复修改不收敛        | 最大循环、Token、时间和 Diff 大小限制                                |
| 执行恶意仓库代码          | Docker 隔离、默认断网、非 root、无宿主凭据                           |
| 队列重复投递              | PostgreSQL 状态、租约、版本号和幂等键                                |
| Tool Result 过大          | Artifact 外置，模型只接收有界摘要                                    |
| Demo 看起来成功但不可证明 | 隐藏测试、回归测试、固定 Commit、重复运行                            |
| 与 Vesta 项目重复         | PatchFlow 聚焦 Coding 场景与量化效果，不重复做通用 Memory/Skill 系统 |

## 19. 后续演进

MVP 完成并有评测数据后，再按真实瓶颈选择扩展：

- GitHub App 接收 Issue，并在审批后创建 PR。
- 基于 Tree-sitter 的符号级代码索引。
- 失败轨迹检索与历史修复经验复用。
- 多模型路由和按阶段选择模型。
- 远程 Runner 与更强的容器隔离。
- PR Review、测试生成和依赖升级等新工作流。

暂时不为“看起来技术更多”而加入 Go、Kubernetes、向量数据库或多 Agent。只有评测数据证明 TypeScript Worker 或现有检索方式成为瓶颈时，才引入新的基础设施。

## 20. 官方技术参考

- [Node.js Releases](https://nodejs.org/en/about/previous-releases)
- [Next.js Blog / Releases](https://nextjs.org/blog)
- [Fastify Documentation](https://fastify.dev/docs/latest/)
- [BullMQ Documentation](https://docs.bullmq.io/)
- [Drizzle ORM Documentation](https://orm.drizzle.team/docs/overview)
- [OpenTelemetry JavaScript](https://opentelemetry.io/docs/languages/js/)
- [Docker Resource Constraints](https://docs.docker.com/engine/containers/resource_constraints/)
