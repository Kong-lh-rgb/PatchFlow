{
  "name": "@patchflow/model-providers",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "PatchFlow 模型接入层：OpenAI/Anthropic 官方 SDK 的 Provider Adapter 契约（Foundation 阶段不实现调用）",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
